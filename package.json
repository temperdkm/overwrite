{
  "name": "overwrite",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "python -m http.server 8080"
  },
  "devDependencies": {
    "vitest": "^2.1.8",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.1"
  }
}
